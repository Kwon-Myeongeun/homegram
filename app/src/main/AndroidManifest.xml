<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".HomegramApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Homegram"
        tools:targetApi="31">
        <activity
            android:name=".presentation.ui.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity">
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="homegram.com"
                    android:path="/invite"
                    android:scheme="https" />
                <data
                    android:host="homegram.com"
                    android:path="/invite"
                    android:scheme="http" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.ui.signin.SignInActivity" />
        <activity android:name=".presentation.ui.main.MainActivity" />
        <activity android:name=".presentation.ui.setting.SettingActivity" />
        <activity android:name=".presentation.ui.invite.InviteLinkActivity" />
        <activity android:name=".presentation.ui.setting.UserPreferenceActivity" />
        <activity android:name=".presentation.ui.main.daily.detail.DailyDetailActivity" />
        <activity android:name=".presentation.ui.main.daily.composition.DailyCompositionActivity" />
        <service
            android:name=".util.notification.NotificationService"
            android:directBootAware="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_baseline_mail_outline_24" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />
    </application>

</manifest>